export type User = {
  id?: number;
  firstName: string;
  lastName: string;
  email: string;
  avatar?: string;
};

export type TechInfo = {
  name: string;
  description?: string;
  link: string;
};
